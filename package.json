{
  "name": "yep-code",
  "version": "2.0.0",
  "description": "Bring real code to your workflows. YepCode lets you run full processes or quick dynamic scripts using Node.js or Python, with support for any NPM or PyPI dependency. All in a secure, serverless environment — no ops, just execution.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive -t 10000"
  },
  "engines": {
    "node": ">=v16",
    "npm": ">=5.6.0"
  },
  "dependencies": {
    "@yepcode/run": "1.3.0",
    "zapier-platform-core": "17.0.2"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "should": "^13.2.0"
  },
  "private": true,
  "zapier": {
    "convertedByCLIVersion": "14.0.0"
  }
}
